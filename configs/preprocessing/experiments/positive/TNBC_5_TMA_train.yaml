# @package _global_
defaults:
  - /experiments/positive/base.yaml

preprocessing:
  positive: true 
  mask_uri: ???
  tiles_uri: ???

metadata:
  run_name: Filtering positive tiles from Triple negative breast cancer TMA (slide TNBC-BF-5-CKAE-PNG.mrxs) train.
  description: "Removing negative and inappropriate tiles by a threshold value."
