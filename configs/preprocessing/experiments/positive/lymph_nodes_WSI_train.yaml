# @package _global_
defaults:
  - /experiments/positive/base.yaml

preprocessing:
  positive: false
  mask_uri: null
  tiles_uri: ???

metadata:
  run_name: Prepare negative tiles from Lymph nodes WSI train.
  description: "Add class_id atribute to tiles."
