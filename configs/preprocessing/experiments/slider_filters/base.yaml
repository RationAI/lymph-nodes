# @package _global_
defaults:
  - _self_
  - override /scripts: slides_filter
  - override /mlflow: kubas_cluster
  - override /metadata: base
  - override /hydra: default

preprocessing:
  tiles_uri: ???
  slides: ???

metadata:
  experiment_name: Lymph nodes
  user: mazgut