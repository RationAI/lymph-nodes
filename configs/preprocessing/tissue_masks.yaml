# @package _global_

mpp: 4.0
max_concurrent: 10
artifact_path: tissue_masks

metadata:
  run_name: Tissue Masks ${dataset.name}
  description: Tissue masks for ${dataset.name}
  hyperparams:
    mpp: ${mpp}
