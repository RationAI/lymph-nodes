# @package _global_


mpp: ???
tile_extent: ???
stride: ???
data_name: ???

tissue_mask_uri: ???
blur_mask_uri: ???
cytokeratin_mask_uri: ???

min_tissue_coverage: 0
max_blur_coverage: 0

metadata:
  run_name: Tiling ${dataset.name}
  description: Tile extraction for ${dataset.name}
  hyperparams:
    mpp: ${mpp}
    level: ${level}
    tile_extent: ${tile_extent}
    stride: ${stride}
    tissue_mask_uri: ${tissue_mask_uri}
    blur_mask_uri: ${blur_mask_uri}
    cytokeratin_mask_uri: ${cytokeratin_mask_uri}