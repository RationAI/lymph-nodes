defaults:
  - hydra: default
  - logger: mlflow
  - /data/datasets@data.predict: tile_embeddings/tile_embeddings
  - /data/embeddings/uris
  - _self_

seed: 42
mode: predict
checkpoint: ???
foundation: prov-gigapath
level: ???
mil_type: abmil  # one of: max_pooling | abmil | transmil | clam

embeddings_uri: ${uris.test_embeddings.${key:${foundation}}.${level}}

trainer: {}

data:
  batch_size: 1
  num_workers: 4

model:
  _target_: lymph_nodes.attention_mil_lymph_nodes.LymphNodesMIL
  mil_type: ${mil_type}
  foundation: ${foundation}

metadata:
  experiment_name: 'Lymph Nodes'
  run_name: '🔮 Predict: ${foundation} ${level}'
  description: 'MIL Predict for Lymph Node Metastasis'
  hyperparams:
    foundation: ${foundation}
    level: ${level}
