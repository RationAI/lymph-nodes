# @package _global_
defaults:
  - /experiments/base.yaml

metrics:
  tiles_uri: ???
  mask_uri: ???

metadata:
  run_name: Calculating metrics for VGG16 bad on the evaluation dataset.
  description: "Calculating metrics."
