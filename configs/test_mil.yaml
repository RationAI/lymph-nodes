defaults:
  - hydra: default
  - logger: mlflow
  - /data/datasets@data.test: tile_embeddings/tile_embeddings
  - /data/embeddings/uris
  - _self_

seed: 42
mode: test
checkpoint: ???
foundation: ???
level: ???
embeddings_uri: ${uris.test_embeddings.${key:${foundation}}.${level}}

trainer: {}

data:
  batch_size: 1
  num_workers: 4

model:
  _target_: lymph_nodes.attention_mil_lymph_nodes.LymphNodesMIL
  foundation: ${foundation}

metadata:
  experiment_name: 'Lymph Nodes'
  run_name: '🧪 Test: ${foundation} ${level}'
  description: 'MIL Test for Lymph Node Metastasis'
  hyperparams:
    foundation: ${foundation}
    level: ${level}
