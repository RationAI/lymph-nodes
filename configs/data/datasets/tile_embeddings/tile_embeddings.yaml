_target_: lymph_nodes.data.datasets.TileEmbeddings
embeddings_uri: ${embeddings_uri}
padding: false
include_slides: null  # set to a list of slide stems to restrict the dataset to a manual split
tracking_uri: ${source_tracking_uri}
